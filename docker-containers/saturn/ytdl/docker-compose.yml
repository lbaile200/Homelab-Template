services:
  metube:
    image: ghcr.io/alexta69/metube:latest
    container_name: metube
    restart: unless-stopped

    ports:
      - "8083:8081"

    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York

    volumes:
      - /var/www/html/docker/ytdl:/downloads
