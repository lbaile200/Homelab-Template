## main upstream for apache(s)
#### 20250524 Lucas (INITIAL CONFIG)
#### 20260403 I don't know if I need this any longer..? I think modern upstreams defined elsewhere
#### in this folder are handling it...
     ## apache.lucasbailey.www
     upstream lucasbailey.www.8081.cluster {
       keepalive 16;
       server 127.0.0.1:8081 max_conns=24;
     } 
      ## circle-jerks.lucasbailey.www
     upstream circlejerks.www.8803.cluster {
       keepalive 16;
       server 127.0.0.1:8803 max_conns=8;
     } 
#end of upstream - IP may change
